spring.application.name=fitness-platform

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/fitness_app
spring.datasource.username=postgres
spring.datasource.password=postgresql

# Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Show SQL in Console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Spring Mail Configuration (for email functionality)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=b45045943@gmail.com
spring.mail.password=pjwqzyodvjoewjsk
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# JWT Secret Key (example, replace with a secure random string)
#jwt.secret=YourSuperSecretKey
#jwt.expiration=3600000  # Token expiration time in milliseconds (e.g., 1 hour)
